name: flutter_desafio04_movie_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.1
  triple_test: ^1.0.0+1

  cambona:
    path: cambona

  peabiru:
    path: peabiru

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
 
  uses-material-design: true
